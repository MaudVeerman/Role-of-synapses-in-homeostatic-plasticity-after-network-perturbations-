# Test environment for the UTwente report analysis
# Python 3.9.6
#
# Recreate with:
#   python3.9 -m venv .venv
#   source .venv/bin/activate      (Windows: .venv\Scripts\activate)
#   pip install -r Codes/requirements.txt
#
# Note: get_data_FM_final.py's optional batch picker uses tkinter, which ships
# with Python (not pip-installable). On some systems install the OS package
# (e.g. `brew install python-tk@3.9` or `apt install python3-tk`).

# --- Core scientific stack (pinned to the versions in the project .venv) ---
numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
matplotlib==3.9.4
pillow==11.3.0
seaborn==0.13.2
read-roi==1.6.0

# --- Analysis packages imported by the notebooks/scripts ---
# (not present in the .venv snapshot; versions below are compatible with
#  Python 3.9 and numpy 2.0.2 — adjust if you used different ones)
scikit-posthocs==0.9.0     # Dunn post-hoc tests (imported as scikit_posthocs)
scikit-image==0.24.0       # image processing (imported as skimage)
tifffile==2024.8.30        # reading .tif microscopy images

# --- Notebook support (to open/run the .ipynb files) ---
jupyter==1.1.1
ipykernel==6.29.5
